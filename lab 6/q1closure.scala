

object q1closure {
    def main(args: Array[String])
    {
      
      val age = 20
      val toPrint =(name: String)=>("My name is = "+name+" and age is "+age);
      println(toPrint("jaidev"));
    }
}